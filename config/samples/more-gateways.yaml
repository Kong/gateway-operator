---
kind: Gateway
apiVersion: gateway.networking.k8s.io/v1alpha2
metadata:
  name: kong-gateway-sample-2
  namespace: kong
spec:
  gatewayClassName: kong-gatewayclass-sample
  listeners:
  - name: http
    protocol: HTTP
    port: 80
---
kind: Gateway
apiVersion: gateway.networking.k8s.io/v1alpha2
metadata:
  name: kong-gateway-sample-3
  namespace: kong
spec:
  gatewayClassName: kong-gatewayclass-sample
  listeners:
  - name: http
    protocol: HTTP
    port: 80
